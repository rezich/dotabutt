extends layout

block content
	p Match #{match.match_id}
	if match.radiant_win
		p Radiant won!
	else
		p Dire won!
	
	for player in match.players
		p #{heroes[player.hero_id].LocalizedName} (#{player.account_id == 4294967295 ? 'Anonymous' : player.summary.personaname}) had #{player.kills} kills,  #{player.deaths} deaths, and #{player.assists} assists